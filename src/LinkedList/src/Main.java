//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
void main() {
    /*
    LinkedList list1 = new LinkedList();

    // Adding nodes at the end
    list1.add(new int[]{12, 4, 33, 21, 7, 87});
    IO.println("Printing list1... ");
    list1.traverse();
    IO.println("List size: " + list1.getSize());


    IO.println("After adding at index 0");
    list1.addAt(new Node(66), 0);
    list1.traverse();
    IO.println("List size: " + list1.getSize());


    // Removing a node from the end

    list1.remove();
    IO.println("After removing a node from the end");
    list1.traverse();
    IO.println("List size: " + list1.getSize());

    // Adding a node at a specified index
    list1.addAt(new Node(5), 0); //Beginning
    //list1.addAt(new Node(16), 6); // The end
    IO.println("After adding a node at index 0 and 7");
    list1.traverse();
    IO.println("List size: " + list1.getSize());

     */


//    LinkedList list2 = new LinkedList();
//    list2.traverse();
//    IO.println("List size: " + list2.getSize());
//    IO.println("Adding an element at index 0...");
//
//    list2.addAt(new Node(55), 0);
//    list2.addAt(new Node(66), 0);
//    list2.traverse();
//    IO.println("List size: " + list2.getSize());

//    IO.println("Clearing the list...");
//    list2.clear();
//    list2.traverse();
//    IO.println("List size: " + list2.getSize());

//int data = 55;
//IO.println("Finding the index of " + data + "... :" + list2.find(0));

    LinkedList list3 = new LinkedList();
    //list3.add(new Node(15));
    list3.remove();
    list3.traverse();
}
